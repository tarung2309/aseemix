// Note: section-label styles are defined in globals.css as a utility class.
// This file documents the pattern for reference.
// Usage: <div className="section-label">Label Text</div>
// The .section-label class is defined in globals.css via @layer components or inline via Tailwind arbitrary.
// For now we define it directly inline in each section component.
export {};
